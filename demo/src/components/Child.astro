---
import type { HTMLAttributes } from "astro/types";
export declare interface Props extends  HTMLAttributes<"input"> {
  label?: string;
  formElementName: string;
}

const { e, ...attrs } = Astro.props;
---


<script>
 
  /* import { FormElement } from 'astro-form-elements'
  import { tap } from  'rxjs'

    const test = new FormElement('test');
    test.valueChanges().pipe(
      tap((x: any) => {
       //console.log(x)
      })
    ).subscribe((x: any) => {
      console.log(x)
      
    }) */
</script>