---
//https://docs.astro.build/en/guides/typescript/#built-in-html-attributes
import FormElement from './FormElement.astro';
import type { FormElementEmitterEvents, FormElementProps } from '../types.d'



/* export declare type InputInterface = Omit<FormElementProps, 'events' | 'type' >  */
/*
 events?: Exclude<FormElementProps["events"], 'change'>,  
  type: Exclude<FormElementProps["type"], 'checkbox' | 'radio'> 

*/

export declare interface Props extends FormElementProps {}


const { 
  events,
  formElementName,
  label,
  ...attrs 
} = Astro.props;

let emitOn: FormElementEmitterEvents[] | undefined = events;
if(!emitOn || !emitOn.length) {
  
}


---
<FormElement formElementName={formElementName} events={emitOn}>
  { !!label ? <label for={formElementName}>{label}</label> : ''}
  <input data-elementInput   name={formElementName}  {...attrs} />
</FormElement>

<script>
  
  
  
  
</script>

