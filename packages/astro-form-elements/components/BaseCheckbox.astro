---
//https://docs.astro.build/en/guides/typescript/#built-in-html-attributes
import type { ElementProps } from '../types.d'
import FormElement from './FormElement.astro';
export declare interface Props extends ElementProps {}

const { 
  emitOnFocus = true, 
  emitOnInput = false, 
  emitOnUpdate = true, 
  emitOnChange = true, 
  ...attrs 
} = Astro.props;



---
  <FormElement formElementName={attrs.formElementName}>
    { !!attrs.label ? <label for={attrs.formElementName}>{attrs.label}</label> : ''}
    <input data-elementInput id="test"  name={attrs.formElementName} type="checkbox"   />
  </FormElement>

  <script>
    //const checkbox = document.getElementById('astroformelements-checkbox');
  </script>


  